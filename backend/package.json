{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "description": "Express API package (scaffold)",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "lint": "eslint --config ../eslint.config.mjs .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "eslint": "^9.22.0",
    "typescript": "~5.8.2",
    "typescript-eslint": "^8.26.1"
  }
}
